<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.redeyesncode.jetpackcompose">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_jetpack"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_jetpack"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackCompose">
        <activity
            android:name=".uiHome.SplashActivity"
            android:exported="true"
            android:label="@string/title_activity_splash"
            android:theme="@style/Theme.JetpackCompose" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".uiHome.SignUpActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackCompose">

        </activity>
    </application>

</manifest>